使用说明：
使用时 只需要将此六个文件包含在工程中即可。

1. 初始化时调用 Sys.h 里的 Sys_Configuration();函数即可。
2. Sys_Configuration( );函数中有 模拟IIC的初始化，不需要修改此函数。只需要转到其中的参数的宏定义处修改宏定义 即可完成 模拟IIC的引脚初始化。
3. 要获取数据时，只需要调用 MPU6050.h 文件中的 getxxxx( );函数即可其返回值就是读取到的数据。